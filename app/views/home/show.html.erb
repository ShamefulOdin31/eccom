<div class="container">
    <div class="row">
        <div class=col>
            <%= image_tag @game.image, alt: @game.name %>
        </div>
        <div class=col>
            <h3><%= @game.name %></h3>
            <h6><%= @game.price %></h6>
        </div>
    </div>
</div>
